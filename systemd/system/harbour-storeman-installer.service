[Unit]
Description=oneshot unit for installing Storeman

[Service]
Type=oneshot
ExecStartPre=/usr/bin/pkcon -pn repo-set-data harbour-storeman-obs refresh-now true
ExecStart=/usr/bin/pkcon -pny install harbour-storeman
# systemd jumps directly to ExecStopPost, if any ExecStart*= statement failed:
ExecStopPost=systemd-cat -t harbour-storeman-installer -p 3 '[Crit] Failed to install harbour-storeman'
